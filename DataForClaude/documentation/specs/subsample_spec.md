
These are the gneral instructions for code changes I want. In the easy download folder for whicherver dataset I want to run this on, I am going to create a sample_metabolites.yml. It is going to be in the same format as the other metabolite.yml except it will contain a smaller subset. In the begining it is probably going to just contain two metabolites and their corresponding transporters. The file will be next to the other .yml files in harreman_outputs.

Now the main goal of what we are going to build is training the model on different permutations of the gene pairs. The first step is a little script or notebook that runs a permutation on the gene pairs. It should probably be a really simple function in a notebook where we give it the dataset path. We are going to initially be working with Alexi UC. The arguments/settings are going to be number of permutations and p. Then the script creates a folder in easy download/ harreman_outputs called subsamples and in it it will iterate with each run to make a run_number_i folder where i starts at 1 and it just increases the max existing i by one for each successive run. In this folder we will have a list of sampled_metabolites_j.yml where j corresponds to the sample number. Each of these files is basically generated using a bernoulli random variable model. So it iterates through each gene pair and adds it to the sample with prob p and doesn't with prob 1-p. If non are selected it does it again instead of creating an empty file. If a gene pair is selected for a metabolite, it is kept, if one is not selected it is just deleted. The file will be in the same format as metabolites.yml. Keep the code for this as simple as possible.

Now we need to create a slurm run loop, probabily dispatched from the same notebook that did the permutations. You need to write a .py file in space travlr that runs the permutation that we can reference. Then in the notebook I want a basic slurm call like the ones we do for other situations. It simply takes the name of the function that you wrote, along with a variable called run that tells it what run to use. If the run isn't specified or is -1, then it is the largest one. I want to be able to specify the dataset as a variable as well. I will select the cors etc. Use the usual gpus we dispatch to and default to 6 cpus but I can change it. I also want a varible called overwrite that says if we run space travler setup with overwrite; default is false. This is litteraly just set in the slurm call in a cell.

The function that slurm runs has the following responsabilities. We create a folder in the dataset folder called spacetravler_subsamples if it doesn't exist yet. Then it creates a folder run_{run}. Then we store the information related to each subsample in subsample_{i} folder. The function iterativly goes through and runs spacetravlr storing the setup in the relevant folder ie one within the subsample_{i} folder. It runs through this linearly. It marks once the sample run is done. The function just picks back up running space travlr on the next sample without a folder or the first one that isn't marked as complete with some indicator file. (I need you to put a little function in the notebook to remove the indicator files in the case I want to rerun).

Now we answer two important questions: How do we run space travlr?
For these runs, I want the gene pairs to be considered like a metabolite. We updated space travlr to aggregate the gene pairs into one metabolite. I don't think you need to update space travlr to do this as the function parses the .yml files, then formats the dict or whatever it gives to space travler to tell it that each gene pair is call {Metabolite}-{gene pair} that only has one corresponding gene pair, ie itself. So to be clear the end coeffiecients will be called metab@{Metabolite}-{gene pair}. The intened behavior is to continue aggregating both directions of the gene pair together. Again I don't think this requires any space travlr changes. If it does we need to talk about exaclty what you are doing and it would need to be minimal and continue supporting the other behavior.

After we are done with all the runs it is time to perform some analysis. In the run_{i} folder, I want you to put an adata object with the metab beta coefficients corresponding to each run, you will need to index this by the run number. We will perform analysis on this.
